import React from 'react';
import { Typography } from '@material-ui/core';

type WorldMapProps = {};

const WorldMap: React.FC<WorldMapProps> = (props: WorldMapProps) => {
  return <Typography>WorldMap</Typography>;
};

export default WorldMap;
