import React from 'react';
import { Typography } from '@material-ui/core';

type TimeLineProps = {};

const TimeLine: React.FC<TimeLineProps> = (props: TimeLineProps) => {
  return <Typography>TimeLine</Typography>;
};

export default TimeLine;
